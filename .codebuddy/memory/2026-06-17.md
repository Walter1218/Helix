## 2026-06-17
- 完成 IDE 集成执行方案（docs/ide-execution-plan.md）。
- 基于项目现有资产（Web UI + Server API + IDE 检测 + 记忆系统），采用 VS Code 扩展路线，分 4 阶段实施（Week 1-8）。
- 核心策略：复用 `packages/app` 的 Web UI 作为 Webview 面板，复用 `packages/opencode/src/server` 的 HTTP API 作为通信层，新建 `packages/vscode-extension/` 作为 VS Code 扩展项目。
- 本周可启动：初始化扩展项目、打通 Webview→Host→mimo serve 链路、上下文收集 MVP。